# Agent Instructions

When finishing a feature:
- Update `TODO.md` if relevant.
- If the feature is major, add it to a `## Features` section in `README.md`.
- Add tests and docs when it makes sense.
- Commit frequently with clear, scoped messages.
